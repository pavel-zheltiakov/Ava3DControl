// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.10",
  "name": "Ava3DControl 12.1.0-preview.10",
  "published_at": "2026-09-04T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/Ava3DControl/releases",
  "body": "# Faster frames, and a surface that does not tile\n\nEverything built against `12.1.0-preview.9` still compiles. Nothing draws differently unless you ask.\n\n## Added\n\n- **Off-screen geometry is skipped.** Nothing to switch on. A scene seen from inside a room stops submitting the rest of the building — 1,017 draws to 342 on the demo's film. `Scene.FrustumCulling` turns it off to compare.\n- **Instancing.** `MeshNode.Instances` draws one mesh many times in one call, each copy with its own transform and tint. Four hundred crates become one draw.\n- **Static batching.** `Batching.Fold` merges a group's anonymous static surfaces into as few draws as they can be drawn in, and says how many it removed.\n- **Spatial queries.** `Node.Query` finds what a box touches or a ray meets, on any subtree. Picking uses it and is faster for it.\n- **Projected texture coordinates.** `Material.UvSource` takes a material's coordinates from a projection instead of from the mesh. No seam, no pole, no stretch, and a density in repeats per metre that is the same number on every surface.\n- **A texture transform that turns.** `Material.UvRotation` and `UvPivot`, and the same three at node scope on `MeshNode` — so forty crates can wear forty phases of one material.\n- **The detail layer.** `Scene.Detail` holds one field of noise for the whole scene; any material can take some of it with `DetailNormal`, `DetailRough` and `DetailTint`. Sub-millimetre surface without sub-millimetre textures.\n- **`Texture.DetailField`** builds that field, and **`Texture.NormalFromHeight`** turns a height map into a normal map.\n- **`Primitives.Box`** takes a chamfer. An edge with a facet on it catches a light; an infinitely sharp one catches nothing.\n- **Block-compressed textures.** `Texture.Compressed` and `Ktx2Loader.Load` take BC, ETC2 and ASTC bytes and hand them to the card as they are. A texel stops costing four bytes.\n\n## Worth knowing\n\n- Culling never hides a shadow. Something behind the camera still casts into the view.\n- A projection travels with the object by default. World space is opt-in, because a world projection makes anything that moves swim.\n- A projected material needs no tangents and no texture coordinates, so generated geometry can wear a normal map.\n- The detail layer fades on its texel footprint rather than on distance, and what it loses becomes roughness — so a surface does not go smooth at range.\n- Instancing is many copies of one mesh; folding is many meshes sharing one material. A room wants both.\n- The CPU renderer projects per vertex and picks one axis rather than blending three, and leaves the detail layer to `OfflineRenderer`. The renderer panel says which backend does what.\n- A device that cannot take a compressed format decodes BC1, BC3, BC4 and BC5 and draws the same picture. BC7, ETC2 and ASTC have no decoder here — ship one of the four, or an uncompressed texture, for a target that lacks them.\n\n**This is a beta.** `12.1.0-preview.10` needs `--prerelease` to install.\n\nDocs: https://pavel-zheltiakov.github.io/Ava3DControl/"
}];
