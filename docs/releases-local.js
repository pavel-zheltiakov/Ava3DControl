// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.11",
  "name": "Ava3DControl 12.1.0-preview.11",
  "published_at": "2026-09-06T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/Ava3DControl/releases",
  "body": "# A light that points, and edges that hold still\n\nEverything built against `12.1.0-preview.10` still compiles. One thing draws differently — see the first line under Fixed.\n\n## Added\n\n- **`SpotLight`.** A light with a position, a direction and a cone: a torch, a stage lamp, a headlight.\n- **A spot light's shadow follows its beam**, so what is outside the cone is unlit rather than lit as though nothing blocked it.\n- **A point light now shadows in every direction**, through a cube map, instead of down one cone aimed at the middle of the scene.\n- **`Scene.ShadowCasters`.** Names the subtree that casts, so one room's shadows are not spread over a whole building.\n- **`Ava3DView.RenderScale`.** Renders more pixels than are shown and averages them down, which smooths edges on every renderer.\n- **`Ava3DView.SampleCount`.** Multisampling at two, four or eight samples per pixel, on Metal and OpenGL.\n- **`RenderInfo` answers four more questions**: how many lights this renderer will draw, how many samples it is taking, how much it is rendering per pixel shown, and how many normal maps it could not apply.\n- **`RenderInfo.ShadowSummary`.** Says which shape the shadow map took and how large one of its texels is.\n\n## Fixed\n\n- Fixed lighting on anything scaled unevenly — a stretched box lit its chamfers from a direction they do not face. Scenes built from stretched primitives will look different, and right.\n- Fixed a point light's shadow sweeping across the scene as the light moved, and leaving whole directions unshadowed.\n- Fixed shadows swelling, detaching and re-attaching as a light moves through a scene.\n- Fixed a lit surface banding itself where a shadow map covers a lot of world.\n- Fixed the detail layer sparkling in the distance on a scene whose field is larger than the default.\n\n## Worth knowing\n\n- Anything carried through a room in somebody's hand wants to be a `SpotLight` — its shadow map is its own beam, which is sharper than a bulb's six faces for the same budget.\n- `LightCollection.Capacity` is a floor and not a limit — `RenderInfo.LightCapacity` is what the renderer you got will actually draw.\n- `SampleCount` smooths edges and nothing else; a surface that shimmers wants `RenderScale`.\n- Vulkan and the software renderer do not multisample yet, and `RenderScale` works on both.\n- A normal map on a mesh with no tangents is still not applied — `RenderInfo.NormalMapsDropped` now counts them, and `Mesh.WithGeneratedTangents()` is the fix.\n- One light casts. A sun through a window and a lamp in the hand still compete for the same map.\n\n**This is a beta.** `12.1.0-preview.11` needs `--prerelease` to install.\n\nDocs: https://pavel-zheltiakov.github.io/Ava3DControl/"
}];
