// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.3",
  "name": "Ava3DControl 12.1.0-preview.3",
  "published_at": "2026-08-06T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/Ava3DControl/releases",
  "body": "Three things a game port asked for, and the operations each one implied that it did not have to ask for.\nAdditive — code built against `12.1.0-preview.2` still compiles and renders the same.\n\n## New\n\n- Environment maps: one image lights the scene and is what metal reflects, on all three renderers.\n- Depth bias, so lines drawn on a surface stop fighting with it.\n- Faceted shading as a mesh operation, and smoothing as the way back from it.\n- Partial spheres, cylinders, tori and discs; planes you can subdivide.\n- Nodes can be moved from one parent to another, and both collections do insert and remove-at.\n- Eight more demo scenes. One per new feature, plus a populated ATX motherboard with an indicator panel\n  joined to it by a flex jumper — 610 nodes, 420 of them the same four meshes — and four ways of looking\n  at it: shaded, as an inspector you can click a component in, as a drawing-office print, and as a\n  wireframe.\n- `Ava3DView.SoftwareThreading`, which holds the CPU renderer's vertex pass to one core when you turn it\n  off. On by default and no effect on the GPU backends. It exists because whether sharing that pass pays\n  depends on the scene and the platform in ways no default can guess, and the only useful answer is the one\n  you measure — so the demo lists both as renderers, named by the cores each is given: \"CPU (×16)\" and\n  \"CPU (×1)\" on a sixteen-core machine. You switch between them and watch the counter.\n- `RenderInfo.RenderMilliseconds` — how long this control's own render took, beside the interval between\n  frames. Frames arrive on the display's clock, so \"60 fps\" cannot tell a renderer with ten times the\n  headroom it needs from one that is only just keeping up. This is the number that can: the motherboard\n  scene on Metal reads 8.4 ms a frame and 0.6 ms of work, so the other 7.8 ms is waiting for the screen.\n- The demo's panel reads its rate off one of two clocks and says which — `8.34 ms /` and a\n  **frame | render** switch. Both are frames per second; what changes is the clock they are divided by.\n  It opens on *frame*, because that is the one that was measured.\n- Every live reading in that panel is three significant figures in tabular figures, anchored against a\n  fixed right edge, so a number that changes sixty times a second is read rather than tracked.\n- The panel is arranged by what each number is about: this run above the rule, the model below it in one\n  column, features last under a gap. One label column runs down the whole of it.\n\n## Fixed\n\n- Adding a node that already had a parent left it drawn twice, in the wrong place.\n- A loop in the scene graph hung the renderer instead of being refused.\n- `GltfLoader` silently replaced any untextured base colour darker than 0.04 luma with a mid grey. Black\n  plastic, epoxy chip packages and anything else authored near black came back at 0.25 and there was no\n  way to ask for what the file said. It now loads the colour the material asks for.\n- `GltfLoader` read glTF's `alphaMode` for `MASK` and ignored `BLEND`, so a transparent material in a\n  file loaded as opaque. It now sets `Material.Blend` and turns `DepthWrite` off with it.\n- `GltfLoader` read glTF's `doubleSided` for shading only, so every loaded model drew its own back faces.\n  A depth buffer hides that, and the CPU fallback had none at the time: the underside of a 1.6 mm board\n  was competing with its own top face for the same pixels. It now sets `Material.Cull` from the same flag.\n- The CPU fallback sorted triangles by their centre, which puts a large one in front of everything\n  standing on the near half of it. It sorts by the farthest corner now — which is also 2.6× faster on the\n  motherboard scene, because triangles that sort into their proper order also batch into far longer runs.\n- The CPU fallback read its own sort keys through the permutation it had just sorted them by, which put\n  every line and point cloud at an arbitrary depth in the drawing order.\n- The CPU fallback decided whether to cull a triangle by pushing all three of its corners through the\n  world matrix again — three matrix multiplies per face, every one repeating work done a few lines\n  earlier — and then lit every vertex in the mesh whether a surviving triangle reached it or not. It now\n  culls from positions already placed, and lights only what is left. On hard-edged geometry, where every\n  face has its own vertices because it has its own normal, that is close to half of them.\n- The CPU fallback now places and lights the whole frame's vertices across every core, and emits the\n  triangles on one. Together with the culling fix the motherboard scene went from 33.4 fps to 45.5 on a\n  software-rendered desktop window.\n- `Ava3DView` took pointer capture on every press, including when `IsOrbitEnabled` and `IsPanEnabled` were\n  both off and it had no use for the pointer. A control holding capture receives every pointer event\n  afterwards wherever the cursor is, so a viewport that ignores presses could still make the rest of the\n  window unreachable if the matching release went astray. It now captures only when there is a drag to\n  capture for, and ends the drag on `PointerCaptureLost` — which a release does not raise, and which used\n  to leave the camera ready to jump by however far the cursor had travelled while the pointer was gone.\n- Opening the demo from disk left it on *Starting…* for ever. It has always needed serving over http — a\n  page whose origin is `null` may not fetch the runtime — but the loader that says so is a module, and a\n  module is fetched with CORS, so it never ran to say it. The page now explains itself before the module\n  is asked for.\n\n## The CPU renderer has a depth buffer now\n\nIt used to hand its triangles to Skia and let Skia fill them, which was faster and drew the wrong picture.\nSkia has no depth buffer, so the only way to decide what was in front was to sort back to front and paint —\nand no sort can be right, because \"in front of\" is not a total order over triangles. Sorting by the\nfarthest corner, the best of the cheap heuristics, loses whenever a large flat triangle reaches less far\nthan something tall standing on it. On the motherboard scene that was not a corner case:\n\n- the board painted over the lower half of the memory slots, cut off along a straight line;\n- each heatsink's own base plate painted over its own fins, as a wedge;\n- the M.2 module's board painted a diagonal wedge across its own chips;\n- indicator legs, and the silkscreen, drawn over the board they pass through.\n\nTwo cheaper repairs were measured and both fail. Sorting per object rather than per triangle cannot help\nwhere one mesh holds both the flat thing and the tall thing — which is exactly how a heatsink is built.\nSubdividing large triangles until the heuristic holds needs more triangles than the whole frame budget\nallows: at the threshold required, the board's top face alone exhausts it and nothing standing on the board\nis drawn at all.\n\nSo the fallback now rasterises its own triangles, against its own depth buffer, in horizontal bands across\nevery core. Draws arrive in the order the GPU backends submit them, because with a depth buffer that order\nis already correct. `DepthTest`, `DepthWrite`, `DepthBias` and `AlphaCutoff` all mean here what they mean\non the GPU, and `LineNode` and `PointsNode` are depth-tested rather than painted over what they cross.\n\nIt costs nothing to run, which was not the expected answer. Rasterising in managed code is slower per\npixel than Skia's, and the global per-triangle sort and per-batch buffer shuffling it replaces were not\nfree either. Measured on the same static frames at 2592×1304, milliseconds of render work:\n\n| Scene | Sorted and painted | Depth-tested |\n| --- | --- | --- |\n| Motherboard — 24,231 triangles | 12.17 | 12.17 |\n| Stress test — 128,002 triangles | 15.12 | **13.49** |\n\nWhat it costs is anti-aliasing. Skia smoothed every triangle edge it filled; this fills whole pixels. On a\ndisplay with two device pixels to the logical one that is a fair trade; on one without, edges are harder\nthan the GPU backends'.\n\n## Faster in the browser\n\nThe demo's WebAssembly build is compiled ahead of time now instead of interpreted, which is worth about\nten times the frame rate on the CPU renderer — 4.2 fps to 46.0 on the motherboard scene, where the\nmanaged half of a frame fell from 229 ms to 17. Nothing in the library changed for it; the demo was\nbeing published the wrong way.\n\nAhead-of-time compilation needs `dotnet publish` with the IL trimmer on, and that combination used to\nleave the browser head drawing nothing at all — which is why it had been built rather than published for\nthree releases. The cause turned out to be narrow: GL entry points are bound as\n`[UnmanagedFunctionPointer]` delegates, calling one in WebAssembly needs a trampoline generated for that\nexact signature, and nothing references those delegate types statically, so the trimmer took them and\nthe first GL call brought the runtime down with no message. Five `TrimmerRootAssembly` entries fix it.\nAnyone publishing an Avalonia application that uses this library for the browser wants the same five —\nsee `samples/Ava3D.Demo.Browser/Ava3D.Demo.Browser.csproj`, where they are written down with the\nreason.\n\n## Threads in the browser, and whether they are worth it\n\nThey work. Nothing in the stack refuses them, which was not what we expected to find: the wasm SDK links\nwith `--enable-threads`, SkiaSharp ships a pthread build of its native library and picks it automatically\noff `WasmEnableThreads`, and Avalonia 12's browser backend has a real render worker that takes the canvas\nwith `transferControlToOffscreen` rather than proxying every GL call back to the page. Published with\n`-p:WasmEnableThreads=true`, the demo runs on both renderers, draws correctly, and .NET reports sixteen\ncores instead of one. It costs one megabyte of the download and nothing at all after compression.\n\nCross-origin isolation is required, and a host that cannot set the two headers is not out of the game — a\nservice worker can add them to its own responses. Measured that way on a server sending no headers at all:\nthe first load fails the runtime's `SharedArrayBuffer` assertion, the worker reloads the page, and the\nsecond load has all sixteen cores. The cost is one visible reload on a visitor's first arrival.\n\nWhat it buys is two separate effects pulling opposite ways, and separating them is the only way the result\nmakes sense. Chromium, sixteen cores, Skia CPU renderer, milliseconds of work per frame:\n\n| Scene | Single-threaded build | Threaded, one core | Threaded, all cores |\n| --- | --- | --- | --- |\n| Motherboard — 24,000 triangles | 20.4 | 23.4 | **16.3** |\n| Stress test — 128,000 triangles | **44.0** | 61.1 | 54.0 |\n\nRead the last two columns and sharing works: seven milliseconds off each scene, which is what the vertex\npass is worth. Read the first two and compiling for threads costs three milliseconds on one scene and\nseventeen on the other, because a shared-memory build pays on every access whether a second thread ever\nruns or not — and that tax lands on the serial half of the frame, which is dominated by sorting the\ntriangles back to front. The motherboard sorts 24,000 of them and comes out ahead; the stress scene sorts\n128,000 and does not.\n\nSo it is not that parallelism failed on the big scene. The refund is roughly constant and the tax scales\nwith the scene, and past some size the tax wins. WebGL is unmoved either way at 0.8 ms, because it was\nnever waiting on the processor.\n\nThe demo ships single-threaded, which is the right default for a renderer that does not know what it will\nbe asked to draw.\n\n## Known limits\n\n- No shadows, no skinning, no transparency sorting; binary `.glb` only.\n- Environment maps are eight bits a channel: put the sun in a light, not in the image.\n- The CPU renderer still lights per vertex, still samples only the base-colour map, and no longer\n  anti-aliases its edges. Hidden surfaces are hidden correctly now; the shading is where it always was.\n- Android builds but has not been run on a device.\n- Browser builds need `<WasmBuildNative>true</WasmBuildNative>`, and trimmed ones need the five\n  `TrimmerRootAssembly` entries above or the GL path dies on its first call.\n- A browser tab gets one thread unless you ask for more. .NET on WebAssembly reports a single core until\n  the application is published with `WasmEnableThreads=true` and the page is cross-origin isolated, so by\n  default the CPU renderer's shared vertex pass is a desktop and mobile gain. Threads do work if you want\n  them — see below — but they are a trade rather than a win.\n\n**This is a beta.** `12.1.0-preview.3` is a prerelease and needs `--prerelease` to install.\n\nDocs: https://pavel-zheltiakov.github.io/Ava3DControl/"
}];
