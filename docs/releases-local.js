// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.2",
  "name": "Ava3DControl 12.1.0-preview.2",
  "published_at": "2026-08-05T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/Ava3DControl/releases",
  "body": "Everything in a scene that is not a lit triangle: sprites, lines and points, four lights, blending and draw\norder, materials with rim and night-side terms, textures that skip the PNG round trip, and a camera and nodes\nthat can be aimed rather than positioned by hand. It came out of a game port — a three.js scene brought to\nAvalonia 1:1 — and it is additive: an application built against `12.1.0-preview` recompiles and renders\nidentically, and every feature here works on all three renderers.\n\n## New features\n\n### Sprites, lines and points\n\nThree node types for the things that are not lit triangles. `SpriteNode` is a camera-facing quad sized in\nworld units, `LineNode` is unlit segments, `PointsNode` is a cloud — each with its own blend mode, depth\ncontrol and `RenderOrder`, all ordered together with the triangles in a single pass. A sprite with\n`DepthTest = false` keeps a ship visible as a point of light after its hull has gone sub-pixel, which emissive\ngeometry cannot do, and points with `SizeAttenuation = false` hold a constant size on screen, which is what a\nstarfield needs. `LineNode.Width` works on every renderer now: anything wider than one device pixel is\nexpanded into triangles and sized after the perspective divide, because every mainstream OpenGL driver clamps\n`glLineWidth` to 1 and Metal has no line-width control at all.\n\n### Blending, draw order and four lights\n\n`BlendMode.Opaque`, `Alpha` and `Additive`, with `DepthTest` and `DepthWrite` on materials and on the three\nnew nodes, and `Node.RenderOrder` over the top. Draw order is render order first, then opaque before blended,\nthen back to front by node origin — per object rather than per triangle. `Scene.Lights` takes up to four\nsources now, directional and the new `PointLight`, whose `Range` and `Decay` falloff matches three.js exactly\nso tuning carries across from a scene authored there. `Scene.Light` still works and still means the first\ndirectional light.\n\n### Materials past metallic-roughness\n\n`Material.Unlit` sends base colour straight through with no lighting and no tone map, so a sun disc comes out\nas a flat circle rather than a lit sphere with a terminator across it. `Material.Cull` chooses `None`, `Back`\nor `Front` — `Front` is what a sky sphere seen from inside needs. A Fresnel rim (`RimColor`, `RimPower`,\n`RimIntensity`, `RimLightBias`) gives a shell a limb glow that is brightest sunward, and `EmissiveNightSide`\nmasks an emissive map to the hemisphere facing away from the key light, so a planet's cities stop glowing\nthrough its own daylight. `BumpTexture` takes a height field and needs no tangents, which makes it the better\nchoice on a sphere, where UV tangents degenerate at the poles.\n\n### Textures without a PNG round trip\n\n`Texture.FromPixels` takes raw RGBA. A procedural scene used to PNG-encode every texture in the application\nand PNG-decode it again in the renderer for nothing, and four 1024×512 maps of that is comfortably over a\nsecond of stall in a 32-bit WebAssembly heap. `Texture.FromEncoded` is the counterpart for bytes that really\nare a file. Between them the two factories fail at the line that made the mistake, instead of later and\nsilently, as a surface that renders untextured for a reason only `RenderInfo` knows.\n\n### Aiming a camera, aiming a node\n\nThe camera can be described the way a shot is: `Position` has a setter, `LookAt` aims without moving,\n`LookFrom` does both, `Forward` / `Right` / `Up` give the basis in world space, and `Roll` — with\n`RollToward(up)` — tips the horizon with a banking aircraft's wings. Nodes know where they are:\n`WorldTransform`, `WorldPosition`, `WorldBounds`, `Descendants` and `Find(name)`, plus a `LookAt` that solves\nout whatever the parents are doing, so a turret on a rolling hull still ends up looking at what it was told\nto. Underneath them, `Rotations.FromTo` and `Rotations.LookAlong` build quaternions from directions rather\nthan from angles, and `Spline`, `Ease` and `Scatter` are the curve, the easing and the repeatable randomness\nthat every scene which moves was writing out by hand.\n\n### Geometry that moves, and geometry you can build\n\n`InvalidateGeometry()` on `Mesh`, `LineNode` and `PointsNode` means vertices can change: write into the\narrays, say so, and the backends re-upload the buffers they already hold — no allocation, no new buffer\nnames, nothing freed. That is the whole of a wake, a waveform, a flag or a particle system where every\nfragment goes its own way. `Mesh.GetEdges(creaseAngle)` hands a mesh's edges straight to `LineNode.Positions`\n— the twelve edges of a cube at one end of the parameter, a loaded ship's panel lines at the other.\n`Mesh.Transformed` bakes a matrix into geometry, normals through the inverse transpose and winding reversed\nunder a mirror, and `Mesh.Merge` joins parts into a single draw call.\n\n### Every renderer says what it can do\n\n`RenderInfo.Features` is thirteen rows, the same thirteen everywhere, each supported or not and each carrying\nits reason. It is the answer to \"will my material look the same on every platform\": Metal and desktop OpenGL\ntick all thirteen, a browser tab ticks twelve, and the CPU fallback ticks four and explains the other nine in\nits own words. It replaces `PlatformLimitations`, which is still there, still reads the same way when\nsomething is missing, and is now derived from the list rather than maintained beside it.\n\n### Twenty-four scenes and a sixty-second film\n\nThe demo is the documentation that runs. Ten new scenes do one thing each, with the feature on and off in the\nsame frame wherever that is possible, and each is a single file of about eighty lines that can be copied out\nwhole. The last entry is `Contact`, a sixty-second scripted film — a convoy, an ambush, a docking — holding\n105 draws, 17,358 triangles and a 600,000:1 depth range with every node's transform changing every frame,\nwhich is what makes its frame rate worth quoting.\n\n## Fixed\n\n`Primitives.Sphere` and `Primitives.Plane` were wound the opposite way from `Primitives.Box`, which was\ninvisible until `CullMode` existed and then culled the wrong half; Metal and OpenGL also disagreed about\nwhich winding faces front. `GltfLoader` threw away a model's UVs whenever the file named no texture, so\nassigning a map after loading did nothing. `Node.Bounds` grew with every level of a rotated graph, so *Fit\nview* framed a scene differently from the automatic first fit. `RenderOrder = int.MaxValue` drew first\ninstead of last, having run past the sign bit. `Scene.Light` could throw, and mutated the scene from its\ngetter. The CPU fallback drew every line and point cloud in front of the triangles, ignored\n`SizeAttenuation`, and corrupted a frame whenever its wire buffer grew.\n\n## Measured\n\n`Contact` mid-engagement — 105 draws, 17,358 triangles, every transform changing every frame:\n\n| Platform | Renderer | fps |\n| --- | --- | --- |\n| macOS, Apple M3 Max | Metal | 120.1 |\n| macOS, Apple M3 Max | OpenGL 4.1 | 120.1 |\n| Chrome, WebAssembly | WebGL 2 | 119.9 † |\n| iOS 26 simulator, iPad Pro | Metal | 60.0 (vsync-capped) † |\n| macOS, Apple M3 Max | Skia, CPU | 37.7 |\n\n† Taken before the film gained modelled hulls and engine plumes, and not re-taken since.\n\nThe first four are display-limited rather than GPU-limited. The last is the honest cost of having no GPU, and\nthe reason the other four exist.\n\n## Known limits\n\nThe CPU fallback lights per vertex, sorts back to front instead of testing depth, and carries only the\nbase-colour map — all of it reported through `RenderInfo.Features` rather than left to be noticed.\n`EnvironmentLight` is still the analytic two-colour hemisphere, so metals reflect a gradient rather than a\nroom. No shadows, no skinning, no per-triangle transparency sorting, binary `.glb` only. Android builds but\nhas not been run on a device. Browser builds need `<WasmBuildNative>true</WasmBuildNative>`.\n\n**This is a beta.** `12.1.0-preview.2` is a NuGet prerelease and needs `--prerelease` to install. Pin the\nexact version and nothing will move under you.\n\nDocs: https://pavel-zheltiakov.github.io/Ava3DControl/api/"
}];
