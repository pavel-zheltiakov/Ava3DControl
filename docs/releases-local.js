// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.5",
  "name": "Ava3DControl 12.1.0-preview.5",
  "published_at": "2026-08-11T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/Ava3DControl/releases",
  "body": "Two things a scene could not do before: hold more than four lights, and draw on Vulkan. Additive — code\nbuilt against `12.1.0-preview.4` still compiles and renders the same.\n\n## API\n\n### Scene\n\n- (Changed) `Scene.Lights` takes as many lights as a scene wants. Adding the fifth used to throw.\n- (Changed) `LightCollection.Capacity` is 16. It now means the fewest any renderer will draw, rather than\n  the most a scene may hold. It is a `const`, so recompile to see the new value.\n\n### Renderers\n\n- (Added) `RenderBackendKind.Vulkan`, and a row for it in `RenderInfo.AvailableBackends`.\n\n## Improvements\n\n- A scene can hold as many lights as it likes.\n- An application started on Vulkan draws on its card instead of falling back to the CPU.\n- OpenGL asks the driver how many lights it has room for. Most machines get far more than sixteen.\n- Bump mapping is something every browser and every current phone always has, rather than something they\n  may be missing.\n- Every renderer says how many lights it will draw.\n\n## Known limits\n\n- No shadows, no skinning, no transparency sorting; binary `.glb` only.\n- Vulkan is not available on Apple platforms, where Metal is the GPU path, or in the browser, which offers\n  only WebGL 2. Apple support is planned.\n- Vulkan samples textures without mipmaps or anisotropic filtering, so fine repeating detail at a glancing\n  angle shimmers.\n- Lights are not free. The software renderer costs a pass over every vertex per light, and OpenGL draws\n  sixteen at worst.\n- Environment maps are eight bits a channel: put the sun in a light, not in the image.\n- The software renderer lights per vertex, samples only the base-colour map, and does not anti-alias.\n- Browser builds must be published rather than assembled, and need `WasmBuildNative`; trimmed ones need\n  five `TrimmerRootAssembly` entries.\n- Android builds but has not been run on a device.\n\n**This is a beta.** `12.1.0-preview.5` is a prerelease and needs `--prerelease` to install.\n\nDocs: https://pavel-zheltiakov.github.io/Ava3DControl/"
}];
