// The release being prepared right now, shown on the Releases page before the GitHub release
// exists — it is created when the version tag is pushed. An entry whose tag is already on GitHub
// is ignored, so this never duplicates or overrides a published release.
//
// Generated from RELEASE_NOTES.md by tools/release-feed.py. Edit the notes, not this file.
window.LOCAL_RELEASES = [{
  "tag_name": "v12.1.0-preview.3",
  "name": "Ava3DControl 12.1.0-preview.3",
  "published_at": "2026-08-06T00:00:00Z",
  "html_url": "https://github.com/pavel-zheltiakov/Ava3DControl/releases",
  "body": "Three things a game port asked for, and the operations each one implied. Additive — code built against\n`12.1.0-preview.2` still compiles and renders the same.\n\n## New\n\n- Environment maps: one image lights the scene and is what metal reflects.\n- Depth bias, so lines drawn on a surface stop fighting with it.\n- Faceted shading as a mesh operation, and smoothing as the way back from it.\n- Partial spheres, cylinders, tori and discs; planes you can subdivide.\n- Nodes can be moved between parents, and both collections do insert and remove-at.\n- `RenderInfo.RenderMilliseconds` — how long the control's own render took, beside the interval between frames.\n- `Ava3DView.SoftwareThreading`, to hold the CPU renderer's vertex pass to one core and measure the difference.\n- Eight more demo scenes, including a 610-node ATX motherboard with four ways of looking at it.\n\n## Fixed\n\n- The CPU renderer has a depth buffer now, so hidden surfaces are hidden correctly. It costs nothing to run.\n- The CPU renderer is about a third faster: it culls before it lights, and places vertices across every core.\n- `GltfLoader` now honours near-black base colours, `BLEND` transparency and `doubleSided`.\n- Adding a node that already had a parent drew it twice; a loop in the graph hung rather than being refused.\n- `Ava3DView` no longer takes pointer capture when it has no drag to use it for.\n- The browser demo now says it needs serving over http, instead of sitting on *Starting…* for ever.\n\n## Faster in the browser\n\n- The demo is compiled ahead of time now — about ten times the frame rate on the CPU renderer. The library\n  did not change; the demo was being published the wrong way.\n- Threads work in the browser, but they tax every frame to speed up part of one. The demo ships without them.\n\n## Known limits\n\n- No shadows, no skinning, no transparency sorting; binary `.glb` only.\n- Environment maps are eight bits a channel: put the sun in a light, not in the image.\n- The CPU renderer lights per vertex, samples only the base-colour map, and no longer anti-aliases its edges.\n- Browser builds need `WasmBuildNative`, and trimmed ones five `TrimmerRootAssembly` entries — the demo's\n  csproj has them written down with the reason.\n- Android builds but has not been run on a device.\n\n**This is a beta.** `12.1.0-preview.3` is a prerelease and needs `--prerelease` to install.\n\nDocs: https://pavel-zheltiakov.github.io/Ava3DControl/"
}];
